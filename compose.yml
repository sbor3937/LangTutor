services:
  langtutor:
    build: .
    restart: unless-stopped
    env_file: ${ENV_FILE:-.env}
    ports:
      - "3000:3000"
    volumes:
      - langtutor_data:/app/data
volumes:
  langtutor_data:
